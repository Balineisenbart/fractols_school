


void draw_fractal()
{
    //check which set to draw

    //loop to draw the set
    while (x < WINDOW_SIZE_W)
    {

        while (y < WINDOW_SIZE_H)
        {
            //draw pixel(x, y, color=result of calc_fractol)
        }
    }

}